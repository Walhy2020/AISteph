# v0.4.3 实时录音声纹

## 变更

- 开始录音后，中心圆形状态切换为随实际声音强度变化的动态声纹。
- 录音计时放在声纹正下方。
- 移除录音状态区中的耳机和设备名称。
- FFmpeg 在录音编码链路中计算 RMS 声强，页面仅获取分贝数值，不传输音频内容。
- 只在录音和停止保存期间每 250 毫秒刷新声强状态，空闲时恢复低频状态检查。

## 兼容性与边界

- 不改变录音文件格式、增益设置、资料库结构和播放接口。
- 静音时声纹保持最低高度，不会误显示强声音。
- 声强只存在于当前录音会话内，不写入资料库或日志正文。
