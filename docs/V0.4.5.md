# v0.4.5 录音安全删除

录音资料库的每条录音新增低强调的“删除”按钮。删除前会显示包含录音标题的二次确认，确认后同步清理：

- 原始 Opus 音频文件
- `data/records` 中的统一记录
- `data/queue` 中的处理队列
- `vault/00_Inbox/Review` 中的 Obsidian 待审核笔记

删除接口继续使用本地访问令牌与同源校验，并严格限制记录 ID 及可删除路径。主记录最后删除，若相关文件清理失败，列表项会保留以便排查和重试。
