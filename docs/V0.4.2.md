# v0.4.2 录音增益设置

## 变更

- 删除录音面板顶部的“RECORDER”“开始新录音”和本机保存提示。
- 在录音设备刷新按钮左侧增加齿轮设置按钮。
- 设置面板提供 0 到 24 dB 录音增益，默认 0 dB。
- 增益设置保存在本机浏览器中，后续录音自动沿用。
- 开始录音时由服务端校验增益，并通过 FFmpeg 音量滤镜作用于保存的音频。
- 录音期间锁定设备、增益和刷新操作，避免同一段录音中途改变参数。

## 兼容性

- 未传入增益参数的旧客户端继续按 0 dB 录音。
- 原有录音文件和资料库记录不受影响。
