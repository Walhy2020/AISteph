{
  "name": "aisteph",
  "version": "0.3.0",
  "private": true,
  "description": "Local-first personal information intake and knowledge organization",
  "type": "module",
  "scripts": {
    "start": "node src/cli.js",
    "web": "node src/server.js",
    "test": "node --test",
    "check": "node --check src/cli.js && node --check src/server.js && node --check src/web/app.js && node --check src/core/intake.js && node --check src/core/recorder.js && node --check src/core/inbox.js && node --check src/core/workspace.js"
  },
  "engines": {
    "node": ">=20"
  }
}
